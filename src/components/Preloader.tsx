import first from '../first.gif'
import sekond from '../sekond.gif'
import s from './Preloader.module.css'

export const Preloader = () => {

    return (
        <img className={s.main} src={sekond}/>
    )
}